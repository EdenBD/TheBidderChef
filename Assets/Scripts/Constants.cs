using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Constants : MonoBehaviour
{
    // Start is called before the first frame update
    public const int coinStart = 10;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
